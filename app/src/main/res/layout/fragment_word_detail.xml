<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:background="@color/leearn_word_background_color"
    android:layout_height="match_parent"
    tools:context=".ui.learn.word.WordDetailFragment">

    <include
        android:id="@+id/toolbar"
        layout="@layout/toolbar"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/word_detail_rec"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginLeft="10dp"
        android:layout_marginTop="15dp"
        android:layout_marginRight="10dp"
        android:layout_marginBottom="90dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/toolbar" />

    <androidx.appcompat.widget.AppCompatButton
        android:id="@+id/next_btn"
        android:layout_width="140dp"
        android:layout_height="50dp"
        android:layout_marginEnd="30dp"
        android:background="@drawable/next_btn_bg"
        android:drawableRight="@drawable/ic_right_arrow"
        android:drawableTint="@color/white"
        android:paddingRight="10dp"
        android:text="Next"
        android:textAllCaps="false"
        android:textColor="@color/white"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="@+id/word_detail_rec"
        app:layout_constraintTop_toBottomOf="@+id/word_detail_rec" />

    <androidx.appcompat.widget.AppCompatButton
        android:id="@+id/save_btn"
        android:layout_width="140dp"
        android:layout_height="50dp"
        android:layout_marginLeft="30dp"
        android:background="@drawable/save_btn_bg"
        android:drawableLeft="@drawable/ic_left_arrow"
        android:drawableTint="@color/white"
        android:paddingLeft="10dp"
        android:paddingRight="10dp"
        android:text="Back"
        android:textAllCaps="false"
        android:textColor="@color/white"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="@+id/word_detail_rec"
        app:layout_constraintTop_toBottomOf="@+id/word_detail_rec" />

    <androidx.appcompat.widget.AppCompatButton
        android:id="@+id/finish_btn"
        android:layout_width="140dp"
        android:layout_height="50dp"
        android:layout_marginRight="30dp"
        android:background="@drawable/save_btn_bg"
        android:drawableTint="@color/white"
        android:paddingLeft="10dp"
        android:paddingRight="10dp"
        android:text="Finish"
        android:textAllCaps="false"
        android:textColor="@color/white"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="@+id/word_detail_rec"
        app:layout_constraintTop_toBottomOf="@+id/word_detail_rec" />

</androidx.constraintlayout.widget.ConstraintLayout>